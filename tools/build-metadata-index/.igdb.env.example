# Copy this file to `.igdb.env` (same folder) and fill in your Twitch/IGDB app credentials.
# `.igdb.env` is gitignored — it never gets committed. See the header of build.py.
IGDB_CLIENT_ID=your_client_id_here
IGDB_SECRET=your_client_secret_here
